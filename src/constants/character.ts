/** Max length for character names (WoW-style short names). */
export const MAX_CHARACTER_NAME_LENGTH = 12;

/** Reasonable bounds for optional per-spec gear score values. */
export const MIN_CHARACTER_GEAR_SCORE = 1;
export const MAX_CHARACTER_GEAR_SCORE = 7000;
